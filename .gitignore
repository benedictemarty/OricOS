# Build artifacts
build/*.o
build/*.bin
build/*.map
build/*.lst
build/*.oosobj
apps/*/build/
*.o

# Stamp file SDK install (Makefile target) — local, jamais commité
tools/oricos-sdk/.installed-stamp

# Editor / OS
*.swp
.DS_Store
