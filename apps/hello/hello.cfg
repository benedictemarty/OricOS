# ld65 config pour app OricOS — binaire flat position-independent.
# Charge à BANK_APP:$0200 par le kernel app loader.

MEMORY {
    APP: start = $0200, size = $E000, type = rw, file = %O, fill = no;
}

SEGMENTS {
    CODE: load = APP, type = ro;
}
